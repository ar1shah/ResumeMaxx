[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# startCommand omitted — Dockerfile CMD handles uvicorn; exec-form startCommand
# does not expand $PORT on Railway and breaks the healthcheck.
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
